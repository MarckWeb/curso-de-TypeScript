"use strict";
//los tipos (Type) no se pueden exterder y tampoco nombrar varios con el mismo nombre
const flores = {
    tipo: 'nose',
    nombre: 'rosa',
    aroma: 'lavanda'
};
console.log(flores);
let a;
// Ejemplo de uso
let userData = { name: 'John', age: 30 };
let productData = { productName: 'Laptop', price: 1200 };
